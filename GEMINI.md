# Auto Git Push Rule

Always `git add -A`, `git commit -m "<descriptive message>"`, and `git push` immediately after making and verifying code modifications. 
Do not ask for the user's permission before committing and pushing.
